/**
 * Centralise les classes Tailwind principales pour `QuizResultsView`.
 */
export const QUIZ_RESULTS_VIEW_STYLES = {
  root: "flex min-h-dvh flex-col",
  main: "fl-page-enter mx-auto flex w-full max-w-lg flex-1 flex-col items-center justify-center px-4 py-10",
} as const;
