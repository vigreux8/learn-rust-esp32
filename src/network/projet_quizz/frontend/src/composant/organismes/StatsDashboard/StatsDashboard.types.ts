export type DayBar = { key: string; label: string; count: number; h: number };

export type KpisAgg = {
  total: number;
  good: number;
  ratio: number;
  avgSecLabel: string;
  uniqueQuestions: number;
  sessionsHint: string;
};
